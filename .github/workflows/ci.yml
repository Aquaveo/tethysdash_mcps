name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  python-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: tethysdash_mcp/requirements.lock

      - name: Install pinned dependencies
        run: pip install -r tethysdash_mcp/requirements.lock

      - name: Install test dependencies
        run: pip install pytest pytest-asyncio pytest-mock

      - name: Smoke import
        run: python -c "from tethysdash_mcp.mcp_server import mcp; print('Import OK:', mcp.name)"

      - name: Standalone-independence guard
        run: pytest test_mcp/test_standalone_independence.py -q

      - name: Full contract suite
        run: pytest test_mcp/ -q

  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image (load locally)
        uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          tags: tethysdash-mcps:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Container smoke — start, hit /health, stop
        run: |
          docker run -d --name tethysdash-mcps-ci -p 9000:9000 tethysdash-mcps:ci
          # Poll /health for up to 30 seconds
          for i in $(seq 1 30); do
            if curl -sf http://localhost:9000/health > /dev/null; then
              echo "Healthcheck passed after ${i}s"
              docker stop tethysdash-mcps-ci
              docker rm tethysdash-mcps-ci
              exit 0
            fi
            sleep 1
          done
          echo "::error::Healthcheck did not respond within 30 seconds"
          docker logs tethysdash-mcps-ci
          docker stop tethysdash-mcps-ci || true
          docker rm tethysdash-mcps-ci || true
          exit 1
